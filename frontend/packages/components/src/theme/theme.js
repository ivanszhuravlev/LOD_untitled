import { colors } from "./colors";
import { fontSize } from "./fonts";

export const theme = {
  colors,
  fontSize,
  paddingHorizontal: 12,
  paddingVertical: 16,
  borderRadius: 4,
  marginCard: 16,
};
